@model IEnumerable<SportAssovv.Models.DisciplineAdherent>

@{
    ViewBag.Title = "Index";
}

<style>
    body {
        background-color: #EBE3EF;
    }
</style>


<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Nom de l'adherent
        </th>
        <th>
            Nom de la discipline
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Adherent.Nom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Discipline.NomDiscipline)
            </td>

            @*            @Html.ActionLink("Edit", "Edit", new { disciplineId = item.DisciplineId, adherentId = item.AdherentId }) |
                @Html.ActionLink("Details", "Details", new { disciplineId = item.DisciplineId, adherentId = item.AdherentId }) |
                @Html.ActionLink("Delete", "Delete", new { disciplineId = item.DisciplineId, adherentId = item.AdherentId })
            *@


        </tr>
    }

</table>
<br />
<a href="@Url.Action("AdminAccount", "AdherentAccount")" class="btn btn-primary">Compte Administrateur</a>
